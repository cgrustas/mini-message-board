<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mini Message Board</title>
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
      <% for (let i = 0; i < messages.length; i++) { %>
      <li>
        <h2><%= messages[i].user %></h2>
        <p><%= messages[i].text %></p>
        <time datetime="<%= messages[i].added.toISOString() %>">
          <%= messages[i].added.toLocaleString() %>
        </time>
        <a href="/message/<%= i %>">View</a>
      </li>
      <% } %>
    </ul>
    <a href="/new">New Message</a>
  </body>
</html>
