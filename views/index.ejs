<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
      <% for (msg of messages) { %>
      <li>
        <h2><%= msg.user %></h2>
        <p><%= msg.text %></p>
        <time datetime="<%= msg.added.toISOString() %>">
          <%= msg.added.toLocaleString() %>
        </time>
      </li>
      <% } %>
    </ul>
    <a href="/new">New Message</a>
  </body>
</html>
